import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should build', (WidgetTester tester) async {
    // TODO: Implement widget tests with mocked dependencies (GetIt/Bloc)
    expect(true, isTrue);
  });
}